machine:
  ruby:
    version: 2.2.3
dependencies:
  override:
    - gem install jekyll
    - sudo pip install awscli
    - npm install
    - rm -rf _site
    - jekyll build
    - /bin/bash optimize.sh

deployment:
  staging:
    branch: master
    commands:
    - /bin/bash publish.sh
