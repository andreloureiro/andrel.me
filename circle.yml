dependencies:
  override:
    - gem install jekyll
    - gem install html-proofer
    - sudo pip install awscli
    - npm install
    - jekyll build
    - /bin/bash optimize.sh

deployment:
  staging:
    branch: master
    commands:
    - /bin/bash publish.sh
